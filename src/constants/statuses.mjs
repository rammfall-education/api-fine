export const FINE_STATUSES_ENUM = {
  canceled: 'canceled',
  completed: 'completed',
  pending: 'pending',
  requested: 'requested',
};

export const FINE_STATUSES = Object.keys(FINE_STATUSES_ENUM);
